syntax = "proto3";
import 'shared.proto';

service Sample {
    rpc sample(SampleRequest) returns (SampleResponse);
}

message SampleRequest {
    string uid = 1;
}

message SampleResponse {
    string name = 1;
    shared.Status status = 2;
}
